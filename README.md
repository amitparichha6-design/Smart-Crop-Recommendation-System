# 🌾 Crop Intelligence System

An end-to-end machine learning web application that recommends crops and predicts yield based on location (Indian states).

## 🚀 How It Works

```
User selects State → Soil & Weather auto-fetched from lookup table
                   → Top N crops recommended (Random Forest Classifier)
                   → Yield predicted per crop (XGBoost Regressor)
                   → Revenue estimated from market prices
```

## 📁 Project Structure

```
crop-recommendation-app/
│
├── app.py                              # Streamlit web application
├── requirements.txt                    # Python dependencies
├── README.md
│
├── utils/
│   └── lookup_data.py                  # State-wise soil & weather averages
│
├── models/                             # Generated by notebook (run first!)
│   ├── crop_model.pkl                  # RF Crop Recommender
│   ├── yield_model.pkl                 # XGBoost Yield Predictor
│   ├── label_encoder.pkl
│   ├── scaler.pkl
│   ├── feature_columns.pkl
│   ├── le_state.pkl
│   ├── le_crop_yield.pkl
│   ├── le_season.pkl
│   └── metadata.json
│
└── crop_recommendation_system.ipynb    # Full training notebook
```

## ⚡ Quick Start

### Step 1: Install dependencies
```bash
pip install -r requirements.txt
```

### Step 2: Add your datasets
Place these files in the project root:
- `Crop_recommendation.csv` — Soil/weather features + crop label
- `crop_production.csv` — Historical Indian crop production data

### Step 3: Train the models
Open and run all cells in `crop_recommendation_system.ipynb`  
This saves all model files into the `models/` folder.

### Step 4: Launch the app
```bash
streamlit run app.py
```

## 🌐 Deploy to Streamlit Cloud

1. Push this entire folder to a **GitHub repository**
2. Go to [share.streamlit.io](https://share.streamlit.io)
3. Click **New App** → connect your GitHub repo
4. Set **Main file path** to `app.py`
5. Click **Deploy!**

> ⚠️ **Important:** The `models/` folder must be committed to GitHub after running the notebook locally. Streamlit Cloud won't run your notebook — it only runs `app.py`.

## 📊 Datasets Used

| Dataset | Source | Description |
|---|---|---|
| Crop_recommendation.csv | Kaggle | N, P, K, temp, humidity, pH, rainfall → crop |
| crop_production.csv | Kaggle / data.gov.in | State, District, Crop, Season, Year, Area → Production |

## 🛠️ Models

| Task | Model | Performance |
|---|---|---|
| Crop Recommendation | Random Forest Classifier | ~99% accuracy |
| Yield Prediction | XGBoost Regressor | R² > 0.85 (typical) |

## 🌍 Features

- 28 Indian states supported with soil & climate averages
- 22 crops in recommendation model
- 100+ crops in yield prediction model  
- Interactive Plotly charts
- Revenue estimation using MSP prices
- CSV download of results
- Radar chart for soil/climate profile
